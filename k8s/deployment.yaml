apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: meeting-intelligence-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: meeting-intelligence
  labels:
    app: meeting-intelligence
spec:
  replicas: 1
  selector:
    matchLabels:
      app: meeting-intelligence
  template:
    metadata:
      labels:
        app: meeting-intelligence
    spec:
      containers:
        - name: api
          image: meeting-intelligence:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: meeting-intelligence-secrets
                  key: api-key
                  optional: true
            - name: APP_DB_PATH
              value: /data/app.sqlite3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: meeting-intelligence-data
---
apiVersion: v1
kind: Service
metadata:
  name: meeting-intelligence
spec:
  selector:
    app: meeting-intelligence
  ports:
    - port: 80
      targetPort: 8000
